import React from 'react';
import { storiesOf } from '@storybook/react-native';

import RootContainer from './RootContainer';

storiesOf('Layout', module).add('RootContainer', () => (
    <RootContainer />
));
